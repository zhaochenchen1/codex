# Git 操作规范

## 1. 任务启动阶段
- **自检 (Pre-check)**：在开始任何新任务前，必须检查本地是否有未提交 (uncommitted) 的更改。
- **清理 (Clean workspace)**：确保工作区干净。如果存在未完成的工作，应先进行提交或 stash。
- **同步 (Sync latest)**：在正式开发前，执行 `git pull` 拉取远程仓库最新代码，确保基于最新版本开发。

## 2. 任务执行阶段
- 按照任务需求进行代码编写与测试。

## 3. 任务完成与提交
- **本地验证**：确保代码在本地运行正常，且符合项目标准。
- **询问同步 (Consent to push)**：任务完成后，**必须**询问用户是否需要同步到远程仓库。
- **推送 (Push)**：仅在得到用户明确允许后，执行 `git push`。
- **冲突处理 (Conflict Resolution)**：
    - 如遇冲突，应优先通过 `rebase` 或 `merge` 解决。
    - 解决冲突时需谨慎核对逻辑，确保不破坏已有功能。
    - 解决后需重新测试验证。
