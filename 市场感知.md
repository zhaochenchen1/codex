## 资金&情绪
市场层面：获取 沪市、深市、创业板、科创板四个板块数据。
具体指标：开盘涨跌幅（百分比）、收盘涨跌幅、最高点涨跌幅、最低点涨跌幅、收盘点数、上涨家数、上涨比例、下跌家数、下跌比例、成交额、成交额变化金额（放量多少亿、缩量多少亿）、成交额增长率（放量为正，缩量为负）

**数据获取要求**：
	1、单独生成数据获取脚本，名称为每日资金&情绪获取
	2、按照要求字段以json形式保存数据，json的最外层key为资金情绪，汇总到对应日期的json文件中
**输出要求**：
- 从对应日期的json文件资金情绪部分，按照要求取数据
- 以表格形式展示上述 12 个字段，严格对齐。
- 表格下方需增加【感知总结】段落，从成交额放缩、涨跌家数对比等维度进行深度体感描述。

## 板块
1、涨幅前二十板块、涨幅（单独展示）
2、跌幅前二十板块、跌幅（单独展示）
3、资金流入前十板块、流入资金（单独展示，单位为亿）
4、资金流出前十板块、流出资金（单独展示，单位为亿）
5、逆势板块（单独展示）
	沪市上涨，从跌幅二十板块提取合并成几个大的方向（半导体产业链、AI产业链等）
	沪市下跌，从涨幅二十板块提取合并成几个大的方向（半导体产业链、AI产业链等）
	沪市上涨，从资金流出前十板块提取合并成几个大的方向（半导体产业链、AI产业链等）
	沪市下跌，从资金流入前十板块提取合并成几个大的方向（半导体产业链、AI产业链等）
**数据获取要求**：
	1、单独生成数据获取脚本，名称为板块获取
	2、数据获取内容为板块数据，不应出现个股情况
	3、按照要求字段以json形式保存数据，json的最外层key为板块，汇总到对应日期的json文件中
**输出要求**：
- 从对应日期的json文件板块部分，按照要求取数据
- 严格按照 1-5 段落格式展示，子模块标题清晰。
- 第 5 项【逆势板块】下方需增加【题材归类总结】，基于实战关键词将杂乱板块合并为核心投资主线。

## 沪深创业科创偏好
根据涨跌幅、成交额增长率、近一年位置（当前点数/一年内最高点*100%，例如最高点5000，当前4000，则位置为80%）进行综合排名：
排名规则：涨跌幅从高到低，权重40%；成交额增长率从高到低，权重40%；近一年位置从低到高，权重20%。
**数据获取要求**：
	1、单独生成数据获取脚本，名称为指数获取
	2、按照要求字段以json形式保存数据，json的最外层key为指数，汇总到对应日期的json文件中
**输出要求**：
- 从对应日期的json文件指数部分，按照要求取数据
- 先以表格展示四个指数的维度数据（涨跌幅、增长率、1年位置）。
- 再展示排名结果及综合评分。
- 下方增加【评分感悟】段落，分析当前市场重心的偏移情况。

## 股价偏好
市场层面：获取 沪市、深市、创业板、科创板四个板块数据。
非ST股：
	（0-10]元以下个股上涨数量、下跌数量、上涨比例、下跌比例
	(10-100]元个股上涨数量、下跌数量、上涨比例、下跌比例
	（100-300]元个股上涨数量、下跌数量、上涨比例、下跌比例
	（300-500]元个股上涨数量、下跌数量、上涨比例、下跌比例
	（500-2000]元个股上涨数量、下跌数量、上涨比例、下跌比例
ST股：个股上涨数量、下跌数量、上涨比例、下跌比例

**数据获取要求**：
	1、单独生成数据获取脚本，名称为股价偏好获取
	2、按照要求字段以json形式保存数据，json的最外层key为股价偏好，汇总到对应日期的json文件中
**输出要求**：
- 从对应日期的json文件股价偏好部分，按照要求取数据
## 市值偏好

市场层面：获取 沪市、深市、创业板、科创板四个板块数据。
（0-10]亿以下个股上涨数量、下跌数量、上涨比例、下跌比例
（10-50]亿以下个股上涨数量、下跌数量、上涨比例、下跌比例
（50-100]亿以下个股上涨数量、下跌数量、上涨比例、下跌比例
（100-300]亿以下个股上涨数量、下跌数量、上涨比例、下跌比例
（300-1000]亿以下个股上涨数量、下跌数量、上涨比例、下跌比例
（1000-100000]亿以下个股上涨数量、下跌数量、上涨比例、下跌比例

**数据获取要求**：
	1、单独生成数据获取脚本，名称为市值偏好获取
	2、按照要求字段以json形式保存数据，json的最外层key为市值偏好，汇总到对应日期的json文件中
**输出要求**：
- 从对应日期的json文件市值偏好部分，按照要求取数据
## 数据持久化与缓存
1、**元数据保存**：所有通过 API 获取的原始数据（Metadata）必须以 JSON 格式保存。
2、**命名规范**：文件名以日期命名，例如 `2026-05-12.json`，存储在 `tmp/data_cache/` 目录下。
33、**缓存优先策略**：
    - 脚本启动时，优先检查是否存在对应日期的 JSON 文件。
    - 如果存在，直接读取本地 JSON 数据进行逻辑处理。
    - 如果不存在，再调用 API 获取数据，并同步写入新的 JSON 文件。
